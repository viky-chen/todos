import { AuthForm } from "../../components/AuthForm";

export const Auth = () => {
  return (
    <div
      style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <AuthForm />
    </div>
  );
};
